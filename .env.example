# GitHub API Token (required for gh CLI authentication)
GITHUB_TOKEN=ghp_your_token_here

# GitLab API Token (required for glab CLI authentication)
# GITLAB_TOKEN=glpat-your_token_here

# GitLab Host (for self-hosted GitLab instances)
# GITLAB_HOST=gitlab.example.com

# OpenHands LLM Configuration
# LLM_API_KEY - Primary API key for LLM provider (required)
# You can also use provider-specific keys for backward compatibility:
# LLM_API_KEY=sk-ant-your_anthropic_key_here
# Or: ANTHROPIC_API_KEY=sk-ant-your_key_here
# Or: OPENAI_API_KEY=sk-your_key_here

# LLM Base URL (optional, for custom LLM endpoints)
# LLM_BASE_URL=https://custom-api.example.com

# AWS Bedrock Configuration (for bedrock/ models)
# Authentication via AWS credentials (environment variables, profile, or IAM role)
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here
# AWS_REGION_NAME=ap-south-1
# Or use an AWS profile:
# AWS_PROFILE=your_profile_name
